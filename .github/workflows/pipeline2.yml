name: creating 2nd pipeline
on: 
  push:
  pull_request:
  schedule:
     - cron: '*/5 * * * *' 
  workflow_dispatch:

jobs:
    echo:
       runs-on: ubuntu-latest 
       steps:
         - name: show the trigger  
          ## this are github contexts( github.event_name )
           run: echo "I've been triggered by a(n) ${{ github.event_name }} event."